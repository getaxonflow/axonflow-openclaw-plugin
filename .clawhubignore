# Files excluded from ClawHub publish.
# Uses .gitignore syntax. The published archive contains only runtime
# artifacts + manifest + docs. Dev-only files (tests, scripts, sources)
# are excluded because they are not needed at runtime.
#
# What SHIPS to ClawHub after these rules apply:
#   - dist/                   compiled runtime
#   - openclaw.plugin.json    manifest
#   - policies/               starter policy SQL/README
#   - assets/                 plugin logo and other listing assets
#   - package.json            npm metadata
#   - README.md               listing page content
#   - CHANGELOG.md            release history
#   - LICENSE                 legal
#   - SECURITY.md             vulnerability disclosure policy

# OS / editor cruft
.DS_Store
**/.DS_Store
Thumbs.db
.vscode/
.idea/

# Version control + dependency install
.git/
.gitignore
.gitattributes
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# CI + repo-management config
.github/
.npmignore

# Build configuration (not needed at runtime)
tsconfig*.json
jest.config.*
eslint.config.*
.eslintrc*
.prettierrc*
.editorconfig

# Test files — not needed at runtime.
tests/
test/
__tests__/
*.test.ts
*.test.js
*.spec.ts
*.spec.js
coverage/

# TypeScript sources and source maps — dist/ is what actually runs
src/
dist/**/*.map
dist/**/*.d.ts.map

# Internal scripts (local e2e harness) — not needed at runtime.
scripts/

# Runtime E2E tests — not consumed by the plugin runtime; ship with
# the repo, not the published artifact.
runtime-e2e/

# Skill source files — published separately via ClawHub web UI.
# These are the source-of-record for the governance-policies skill,
# not part of the plugin package.
clawhub/

# Local packaging artifacts
*.tgz

# Meta: don't ship the ignore file itself
.clawhubignore
